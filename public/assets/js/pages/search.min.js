/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 120);
/******/ })
/************************************************************************/
/******/ ({

/***/ 120:
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(121);


/***/ }),

/***/ 121:
/***/ (function(module, exports) {

var search = {
    init: function init() {
        this.bindDaterange();
        this.bindFilters();
        this.bindOrder();
        this.bindSearch();
        this.scrollResults();
    },

    bindDaterange: function bindDaterange() {
        $('input[name="daterange"]').daterangepicker({
            autoUpdateInput: false,
            opens: 'left',
            ranges: {
                'Últimos 7 dias': [moment().subtract(6, 'days'), moment()],
                'Últimos 30 dias': [moment().subtract(29, 'days'), moment()],
                'Este Mês': [moment().startOf('month'), moment().endOf('month')],
                'Mês Anterior': [moment().subtract(1, 'month').startOf('month'), moment().subtract(1, 'month').endOf('month')]
            },
            "locale": {
                "format": "DD/MM/YYYY",
                "separator": " - ",
                "applyLabel": "Aplicar",
                "cancelLabel": "Limpar",
                "fromLabel": "De",
                "toLabel": "Para",
                "customRangeLabel": "Personalizado",
                "weekLabel": "S",
                "daysOfWeek": ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sab"],
                "monthNames": ["Janeiro", "Fevereiro", "Março", "Abril", "Maio", "Junho", "Julho", "Agosto", "Setembro", "Outubro", "Novembro", "Dezembro"],
                "firstDay": 1
            }
        }, function (start, end, label) {
            console.log("Uma nova seleção de data foi feita: " + start.format('DD-MM-YYYY') + ' à ' + end.format('DD-MM-YYYY'));
        }).on('apply.daterangepicker', function (ev, picker) {
            $(this).val(picker.startDate.format('DD/MM/YYYY') + ' - ' + picker.endDate.format('DD/MM/YYYY'));
            $('#searchOrder > select#orderning').change();
        }).on('cancel.daterangepicker', function (ev, picker) {
            $(this).val('');
            $('#searchOrder > select#orderning').change();
        });
    },

    bindFilters: function bindFilters() {
        var _this = this;

        $('a.media, a.year, a.tag, a.category').on('click', function (event) {
            event.preventDefault();
            $(event.target).toggleClass('active');
            _this._filter();
        });

        $('a.allArea').on('click', function (event) {
            $(event.target).parent().parent().find('a.tag').addClass('active');
            _this._filter();
        });
    },

    bindOrder: function bindOrder() {
        var _this2 = this;

        $('#searchOrder > select#orderning').on('change', function (event) {
            event.preventDefault();
            _this2._filter();
        });
    },

    bindSearch: function bindSearch() {
        var _this3 = this;

        var form = $("#searchBox");
        form.find('input#searchInput').keypress(function (event) {
            var keycode = event.keyCode ? event.keyCode : event.which;
            if (keycode == '13') _this3._filter();
        });

        form.find('button#searchBtn').on('click', function (event) {
            event.preventDefault();
            _this3._filter();
        });

        form.find('button#searchClear').on('click', function (event) {
            event.preventDefault();
            $('#searchBox input#searchInput').val("");
            _this3._filter();
        });

        form.find('');
    },

    scrollResults: function scrollResults() {
        var element = $('main.internal h2.infoSearch'),
            elOffset = element.offset(),
            elHeight = element.innerHeight(),
            newEl = element.clone().addClass('fixed');

        $(window).on('scroll', document, function () {
            var scrollTop = $(this).scrollTop();

            if (scrollTop >= elOffset.top + elHeight && $('main.internal h2.infoSearch.fixed').length === 0) {
                $('main.internal .searchLine').after(newEl);
                $('main.internal h2.infoSearch.fixed').animate({ top: 0 }, 300);
            } else if (scrollTop < elOffset.top + elHeight && $('main.internal h2.infoSearch.fixed').length > 0) {
                $('main.internal h2.infoSearch.fixed').animate({ top: "-50px" }, 150, function () {
                    $(this).remove();
                });
            }
        });
    },

    _filter: function _filter() {
        var url = baseUrl + 'pesquisa?k=' + $('#searchBox input#searchInput').val(),
            media = '',
            year = '',
            tag = '',
            category = '';

        // Media
        $('a.media.active').each(function (i, el) {
            return media = media + $(el).attr('href') + '-';
        });
        if (media !== '') url = url + '&m=' + media.substring(0, media.length - 1);

        // Categocry
        $('a.category.active').each(function (i, el) {
            return category = category + $(el).attr('href') + '-';
        });
        if (category !== '') url = url + '&c=' + category.substring(0, category.length - 1);

        // Years
        $('a.year.active').each(function (i, el) {
            return year = year + $(el).attr('href') + '-';
        });
        if (year !== '') url = url + '&y=' + year.substring(0, year.length - 1);

        // Tags
        $('a.tag.active').each(function (i, el) {
            return tag = tag + $(el).attr('href') + '-';
        });
        if (tag !== '') url = url + '&t=' + tag.substring(0, tag.length - 1);

        //periodo
        if ($('input[name="daterange"]').val().length) {
            url = url + '&p=' + $('input[name="daterange"]').data('daterangepicker').startDate.format('YYYY-MM-DD') + ',' + $('input[name="daterange"]').data('daterangepicker').endDate.format('YYYY-MM-DD');
        }

        // Orders
        if ($('#searchOrder > select#orderning').val() > 1) url = url + '&o=' + $('#searchOrder > select#orderning').val();

        // Redirect to Search
        window.location = url;
    }
};

$(document).ready(function () {
    search.init();
});

/***/ })

/******/ });